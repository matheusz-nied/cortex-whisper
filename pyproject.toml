[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cortex-whisper"
dynamic = ["version"]
description = "Private, local AI voice dictation"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{ name = "Matheus Fernandes da Silva", email = "matheusz.nied@gmail.com" }]
maintainers = [{ name = "Matheus Fernandes da Silva", email = "matheusz.nied@gmail.com" }]
dependencies = [
  "faster-whisper>=1.2.1,<2",
  "jeepney>=0.9,<1",
  "numpy>=2.2,<3",
  "platformdirs>=4.3,<5",
  "pynput>=1.8,<2",
  "PySide6>=6.8.6,<7",
  "sounddevice>=0.5.2,<1",
]

[project.scripts]
cortex-whisper = "cortex_whisper.__main__:main"
whisper-ditado = "cortex_whisper.__main__:main"

[project.gui-scripts]
cortex-whisper-gui = "cortex_whisper.__main__:main"

[project.urls]
Homepage = "https://github.com/matheusz-nied/cortex-whisper"
Repository = "https://github.com/matheusz-nied/cortex-whisper"
Issues = "https://github.com/matheusz-nied/cortex-whisper/issues"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.dynamic]
version = { attr = "cortex_whisper.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]

[tool.ruff]
line-length = 110

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
